interface:
  display_name: "Agent Orchestration"
  short_description: "Coordinate agents with explicit lifecycles"
  default_prompt: "Use $agent-orchestration to coordinate bounded agent roles, model bindings, lifecycles, and durable state for this task."
